import React from "react";

const Contacts = () => {
  return (
    <div>
      <h1>Contacts</h1>
    </div>
  );
};

export default Contacts;

Contacts.getLayout = function getLayout(page: any) {
  return page;
};
