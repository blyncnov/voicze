import React from "react";

const Referrals = () => {
  return (
    <div>
      <h1>Referrals</h1>
    </div>
  );
};

export default Referrals;

Referrals.getLayout = function getLayout(page: any) {
  return page;
};
