import React from "react";

const Survey = () => {
  return (
    <div>
      <h1>Survey</h1>
    </div>
  );
};

export default Survey;

Survey.getLayout = function getLayout(page: any) {
  return page;
};
